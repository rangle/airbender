This is a sample app. To serve it, first install http-server:

    sudo npm install http-server -g

Then `cd` into `client` and run http-server in it:

    cd client
    http-server

(You can also use any other HTTP server.)