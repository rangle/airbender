'use strict';

angular.module('airbender.components.graphs.graph-directive', [])

.directive('abGraph', function () {
  var directive = {
    scope: {},
    replace: true,
    template: '<b>Foo</b>'
  };
  return directive;
});